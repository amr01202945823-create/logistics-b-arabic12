import React from 'react';

// This component is intentionally left empty to remove the chatbot functionality as per the user's request.
export const Chatbot: React.FC<{ isOpen: boolean; onClose: () => void; }> = () => null;
